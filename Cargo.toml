[package]
name = "pmatrix-encoder"
version = "0.1.0"
edition = "2021"
description = "Reference encoder for schema conformance. Not an execution engine."
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4", features = ["derive"] }
